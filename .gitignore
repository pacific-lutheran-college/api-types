# Dependencies
node_modules/

# Build output — dist/ is committed in packages/ so consumers can install
# via git dependency without needing a build step.
# Only ignore dist at the root level (no root-level build).
/dist/

# TypeScript cache
*.tsbuildinfo

# IDE files
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock
